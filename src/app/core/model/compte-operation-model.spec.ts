import { CompteOperationModel } from './compte-operation-model';

describe('CompteOperationModel', () => {
  it('should create an instance', () => {
    expect(new CompteOperationModel()).toBeTruthy();
  });
});
