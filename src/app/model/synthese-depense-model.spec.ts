import { SyntheseDepenseModel } from './synthese-depense-model';

describe('SyntheseDepenseModel', () => {
  it('should create an instance', () => {
    expect(new SyntheseDepenseModel()).toBeTruthy();
  });
});
