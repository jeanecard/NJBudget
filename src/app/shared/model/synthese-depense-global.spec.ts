import { SyntheseDepenseGlobal } from './synthese-depense-global';

describe('SyntheseDepenseGlobal', () => {
  it('should create an instance', () => {
    expect(new SyntheseDepenseGlobal()).toBeTruthy();
  });
});
